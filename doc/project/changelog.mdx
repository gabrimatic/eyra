---
title: "Changelog"
description: "Where to track shipped and unreleased Eyra changes."
---

Eyra's source changelog lives in `CHANGELOG.md`.

Current package metadata reports version `4.2.1`. Current `main` is staged for 4.2.1 release approval, with no tag or publish step completed yet.

The 4.2.1 line collects the post-4.2.0 release-candidate work: guided setup and installer repair UX, docs under `doc/` with guarded Pages deployment, Homebrew formula mock test mode, connector privacy enforcement, text-format tool-call recovery hardening, semantic/protocol runtime history isolation, semantic-to-model worker context adaptation, and settled-file handling for file-appears triggers.

`## Unreleased` in `CHANGELOG.md` should stay empty or say there are no unreleased changes while 4.2.1 is being prepared for approval.

## Read the changelog

Repository link:

[CHANGELOG.md](https://github.com/gabrimatic/eyra/blob/main/CHANGELOG.md)

## Update rules

When runtime behavior changes, keep these surfaces aligned:

- `CHANGELOG.md`
- `README.md`
- `.env.example`
- Mintlify docs
- Tests
- Certification notes when voice-to-computer behavior changes

## Release evidence

A release note should state which checks actually ran:

- Unit tests.
- Ruff.
- Lockfile check.
- Setup syntax.
- Package build.
- Runtime smoke.
- Certification matrix.
- Physical microphone challenge when claimed.
- Secret scan when public release scope requires it.
