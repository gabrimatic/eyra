"""Local-first request routing policy.

Import concrete classes from their modules, for example
``runtime.routing.router`` or ``runtime.routing.types``. Keeping this package
initializer light avoids import cycles with tools.
"""
